[project]
name = "shipment-tracker-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { email = "karampelas.ds@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.141.1",
    "psycopg[binary]>=3.3.4",
    "pydantic>=2.13.4",
    "python-dotenv>=1.2.2",
    "sqlalchemy>=2.0.52",
    "uvicorn>=0.52.1",
]

[project.scripts]
shipment-tracker-api = "shipment_tracker_api:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
